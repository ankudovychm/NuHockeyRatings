jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run update script
        run: node script.js
        env:
          GITHUB_PAT: ${{ secrets.NEWPAT }}
